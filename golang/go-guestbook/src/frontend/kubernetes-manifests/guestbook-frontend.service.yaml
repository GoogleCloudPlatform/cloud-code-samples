apiVersion: v1
kind: Service
metadata:
  name: go-guestbook-frontend
  labels:
    app: guestbook
    tier: frontend
spec:
  type: LoadBalancer
  selector:
    app: guestbook
    tier: frontend
  ports:
  - port: 80
    targetPort: http-server
