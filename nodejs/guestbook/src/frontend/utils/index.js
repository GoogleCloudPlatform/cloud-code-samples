const timeAgo = (utcTime, currTime) => {
    // messages.forEach(msg => {
    // const utcTime, currTime = msg.timeStamp
    //   // const date = moment
    // });
}

module.exports = {
    timeAgo: timeAgo    
}